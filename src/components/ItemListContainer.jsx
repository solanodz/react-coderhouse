
export const Greeting = ({greeting}) => {
    return (
        <h1 style={{
            padding:100,
            fontFamily: 'Poppins',
            fontSize: 80
        }}>
            {greeting}</h1>
    );
}